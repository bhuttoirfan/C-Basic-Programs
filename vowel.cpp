#include<iostream>
using namespace std;

int main() {
	char ch;
		
	cout<<"Enter any alphabetical character:";
	cin>>ch;
	
	if(ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u'){
		
		cout<<"it is vowel";
		
	}else {
		cout<<"it is not vowel";
	}
	
		
}